server:
  port: 8080

spring:
#  mvc:
#    view:
#      prefix: /templates/
#      suffix: .html
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/job?useSSL=false
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

swagger:
   enabled: true

springfox:
  documentation:
    swagger:
      v2:
        path: /api-docs


